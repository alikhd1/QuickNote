# Keep line endings identical on every machine. Without this, core.autocrlf rewrites
# files to CRLF on Windows checkouts while the repo and the macOS side stay LF, which
# produces phantom whole-file diffs and breaks exact-match tooling.
* text=auto eol=lf

# Binaries must never be touched.
*.png binary
*.ico binary
*.icns binary
